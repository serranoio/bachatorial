{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rspress build",
    "dev": "rspress dev",
    "preview": "rspress preview",
    "export:stories": "bun run scripts/export-stories-mp4.ts",
    "retest": "rm -rf exports && mkdir exports && bun run scripts/export-stories-mp4.ts",
    "test:export": "bun run scripts/test-single-mp4.ts"
  },
  "dependencies": {
    "rspress": "^1.40.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "playwright": "^1.57.0"
  }
}
